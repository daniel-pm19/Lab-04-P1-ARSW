spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Example PostgreSQL configuration (activate profile 'postgres' to use PostgresBlueprintPersistence)
# spring.profiles.active=postgres
# spring.datasource.url=jdbc:postgresql://localhost:5432/blueprintsdb
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.hibernate.ddl-auto=update

# Swagger UI will be exposed by springdoc at /swagger-ui.html (starter handles it)
